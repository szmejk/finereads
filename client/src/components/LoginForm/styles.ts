import styled, { css } from 'styled-components'

export const CtaText = styled.h1`
  font-size: 32px;
  color: #ffffff;
  text-align: center;
  margin: 0.25em 0;
`

export const Input = styled.input`
  background-color: #ffffff;
  box-shadow: 4px 2px 2px rgba(0, 0, 0, 0.12);
  border: none;
  border-radius: 4px;
  font-size: 1.5em;
  margin-bottom: 0.5em;
`

